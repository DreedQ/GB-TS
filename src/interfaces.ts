export interface SearchFormData{
  searchCity:string,
  dateIn:string,
  dateOut:string,
  maxPrice:number
}

export interface Todo {
  completed: boolean,
  id: number,
  title: string,
  userId: number
}
